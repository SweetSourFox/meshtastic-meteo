# GitHub Actions

This fork does not run upstream Meshtastic CI workflows. Build locally with PlatformIO:

```bash
pio run -e m5stack-cardputer-adv
```

See the root [README](../README.md) for hardware, flashing, and Meteo module layout.
